// 가장 큰 수 찾기
let numList = [1, 12, 35, -12, 24, 64, 123, 2, 6, 7];
let maxNumber = 0;

console.log(maxNumber);


for(i = 0; i < numList.length; i++){

}

console.log(maxNumber);

for(i = 0; i < numList.length; i++){
    if(numList[i] > maxNumber){
        maxNumber = numList[i];
    }
}

console.log(maxNumber);